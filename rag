import React from 'react';
import { Search, Users, BarChart, PieChart } from 'lucide-react';

const NavLink = ({ href, children }) => (
  <a href={href} className="text-gray-700 hover:text-blue-600 transition-colors">
    {children}
  </a>
);

const Button = ({ children, primary }) => (
  <button
    className={`px-4 py-2 rounded-full text-sm font-medium transition-colors ${
      primary
        ? 'bg-blue-600 text-white hover:bg-blue-700'
        : 'bg-white text-gray-700 hover:bg-gray-100'
    }`}
  >
    {children}
  </button>
);

const FeatureCard = ({ icon, title, description }) => (
  <div className="bg-white p-6 rounded-lg shadow-sm">
    <div className="text-blue-600 mb-4">{icon}</div>
    <h3 className="text-xl font-semibold mb-2">{title}</h3>
    <p className="text-gray-600">{description}</p>
  </div>
);

const HomePage = () => {
  return (
    <div className="min-h-screen bg-gray-50 text-gray-900">
      <header className="fixed top-4 left-0 right-0 z-10 px-4">
        <nav className="flex justify-between items-center max-w-7xl mx-auto">
          <div className="bg-white rounded-full shadow-lg px-6 py-3 flex items-center space-x-6">
            <div className="text-2xl font-bold text-blue-600">RAG Software</div>
            <NavLink href="#use-cases">Use cases</NavLink>
            <NavLink href="#features">Features</NavLink>
            <NavLink href="#about">About</NavLink>
          </div>
          <div className="bg-white rounded-full shadow-lg px-4 py-2 flex items-center space-x-4">
            <Button>Log in</Button>
            <Button primary>Book a demo</Button>
          </div>
        </nav>
      </header>

      <main className="pt-24">
        <section className="bg-gradient-to-r from-blue-500 to-purple-600 text-white py-32 px-6">
          <div className="max-w-4xl mx-auto text-center">
            <h1 className="text-5xl font-bold mb-6 leading-tight">
              Make better decisions with RAG Software
            </h1>
            <p className="text-xl mb-8">
              RAG Software is a complete research platform that helps you make better decisions by
              understanding your data better. Our AI Agent conducts hundreds of analyses in
              parallel, providing you with insights in days, not weeks.
            </p>
            <Button primary>Learn more</Button>
          </div>
        </section>

        <section id="features" className="py-20 px-6">
          <div className="max-w-6xl mx-auto">
            <h2 className="text-3xl font-semibold text-center mb-12">Why Choose RAG Software?</h2>
            <div className="grid grid-cols-1 md:grid-cols-2 gap-8">
              <FeatureCard
                icon={<Search size={32} />}
                title="Automate building your studies"
                description="Designed to help you gather insights faster. Use RAG Software's AI to build best-practice research."
              />
              <FeatureCard
                icon={<Users size={32} />}
                title="Survey your audience or our panel"
                description="Recruit respondents from your own audience or our panel of 70m+ users with fine-grained targeting."
              />
              <FeatureCard
                icon={<BarChart size={32} />}
                title="Interview customers with our AI agent"
                description="Conduct hundreds of interviews in parallel with our AI agent, providing you with insights in days, not weeks."
              />
              <FeatureCard
                icon={<PieChart size={32} />}
                title="Make data-backed decisions within days"
                description="Our agent automatically analyzes the data and provides you with insights. Drill down into the data to understand your audience better."
              />
            </div>
          </div>
        </section>

        <section className="py-20 px-6 bg-white">
          <div className="max-w-6xl mx-auto flex items-center">
            <div className="w-1/2 pr-12">
              <h2 className="text-3xl font-semibold mb-6">Go from question to insights in days, not weeks</h2>
              <p className="text-xl mb-8">Let AI generate your study for you and analyze the results, providing you with actionable insights faster than ever before.</p>
              <Button primary>Learn more</Button>
            </div>
            <div className="w-1/2">
              <img src="/api/placeholder/600/400" alt="RAG Software Dashboard" className="rounded-lg shadow-lg" />
            </div>
          </div>
        </section>
      </main>

      <footer className="bg-gray-800 text-white py-12">
        <div className="container mx-auto px-6 text-center">
          <p>&copy; 2024 RAG Software. All rights reserved.</p>
        </div>
      </footer>
    </div>
  );
};

export default HomePage;
